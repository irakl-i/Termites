#include <iostream>
#include <vector>
#include <stdio.h>

using namespace std;

int termites(vector<int> &input) 
{
	int value = 0;
	 
}

int main()
{
	int x = 0;
	vector<int> input;
	cin >> x;
	for (int i = 0; i < x; i++)
	{
		int n;
		cin >> n;
		input.push_back(n);
	}

	cin >> x;
}